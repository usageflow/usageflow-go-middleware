# UsageFlow application key (required)
USAGEFLOW_API_KEY=uf_live_replace_me

# OpenAI key (same as agents/js/examples/.env — required for live LLM replies)
OPENAI_API_KEY=sk-replace_me

# Local chat UI port
PORT=8081
