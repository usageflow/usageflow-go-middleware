# UsageFlow configuration types

This package contains types used by the UsageFlow middleware. Customer route and policy configuration belongs in the [UsageFlow Console](https://console.usageflow.io), not in application code.

See the [customer integration guide](../../README.md) for supported setup and configuration.
